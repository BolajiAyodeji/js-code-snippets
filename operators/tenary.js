//Check points
let points = 200;
let type = points > 110?'gold':'silver';
console.log(type);

//Checks max number
const num = [3, 5]
console.log(num);

let max = num[0] > num[1] ? 'yes':'no';
console.log(max);

